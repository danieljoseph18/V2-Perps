// SPDX-License-Identifier: MIT
pragma solidity 0.8.20;

contract TradeManager {
    // contract for managing open trades and requests
    // decrease positions, close positions, get position data
    // trades opened in MarketStorage, so we can track trades per pool
}